rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permitir acceso total a usuarios autenticados (Admin tiene acceso a todo)
      allow read, write: if request.auth != null;
    }
  }
}
